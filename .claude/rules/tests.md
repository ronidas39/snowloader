Test files are SACRED. Never modify a test to make it pass.
If a test fails, the implementation is wrong, not the test.
Use `responses` library to mock all HTTP calls.
Every test function name starts with test_.
Use descriptive names: test_what_when_then pattern.
Mock data must look like real ServiceNow API responses.
